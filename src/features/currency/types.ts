// Open now — driven by whatever currencies are actually present across the
// dynamic exchange list (see features/market/hooks/use-market-exchanges.ts),
// not a fixed USD/INR pair.
export type AppCurrency = string;
